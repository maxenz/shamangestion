
<h2 id="loginTitle">Inicio de Sesi&oacute;n</h2>

<hr />

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name), :id => 'loginForm') do |f| %>
  <div class="form-inputs">
    <%= f.input :email, :required => false, :autofocus => true %>
    <%= f.input :password, :required => false %>
    <%= f.input :remember_me, :label => 'No cerrar sesión', :id => 'remember_login', :as => :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions" style="background:white;text-align:center">
    <%= f.button :submit, "Ingresar", :class => 'btn btn-primary' %>
  </div>
<% end %>
<div id="lnkNewPassword">
	<%= link_to "Olvidaste el password?", new_user_password_path %> 
</div>


